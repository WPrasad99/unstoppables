import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"], variable: "--font-inter" });

export const metadata: Metadata = {
  title: "AI Bias Monitor — Real-Time Fairness Analytics",
  description:
    "Production-grade AI bias monitoring dashboard. Analyze fairness, detect disparities, and get actionable recommendations in real-time.",
  keywords: ["AI bias", "fairness", "monitoring", "dashboard", "analytics"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
